{
  "name": "cold-mail-generator",
  "version": "1.0.0",
  "description": "Automated Cold Email & Dynamic Resume Tailor application powered by NVIDIA NIM and Google OAuth2",
  "main": "server/src/index.js",
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client --include=dev && npx --prefix server puppeteer browsers install chrome",
    "install:all": "npm install --prefix server && npm install --prefix client --include=dev && npx --prefix server puppeteer browsers install chrome",
    "build": "npm install --prefix server && npx --prefix server puppeteer browsers install chrome && npm install --prefix client --include=dev && npm run build --prefix client",
    "start": "node server/src/index.js",
    "dev:server": "node server/src/index.js",
    "dev:client": "npm run dev --prefix client"
  },
  "keywords": [
    "cold-email",
    "resume-tailor",
    "nvidia-nim",
    "gmail-oauth2",
    "pdfkit"
  ],
  "author": "Santhosh T K",
  "license": "MIT"
}
